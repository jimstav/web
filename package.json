{
  "dependencies": {
    "axios": "^1.8.4",
    "json-server": "^0.17.4",
    "parcel": "^2.14.2"
  },
  "scripts": {
    "start:db": "npx json-server -w db.json",
    "start:parcel": "npx parcel index.html"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
